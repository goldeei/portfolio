'use client';

import { Scene } from '@/components/scene';

import Landing from './(landing)/page';

export default function Home() {
  return (
    <main>
      <Landing />
      <Scene />
    </main>
  );
}
